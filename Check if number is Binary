#include <bits/stdc++.h> 
bool isBinary(string &bin) {
    // Write your code here.
    for(int i=0; i<bin.size(); i++)
    {
        if(bin[i]=='0' || bin[i]=='1' ) continue;
        
        else return 0;
    }
    return 1;
}
